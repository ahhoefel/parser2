import a fiba

func fib(n int) int {
 var x int = 1
 var y int = 1
 if n + (-1) {
   x = a.fib(n + (-1))
   if n + (-2) {
     y = a.fib(n + (-2))
   }
 }
 return x + y
}

func count(n int) int {
 if n {
   return 2 + a.count(n + (-1))
 }
 return 2
}

